const money = value => `$${Number(value || 0).toFixed(2)}`
export default function FeeCard({ paid = 0, pending = 0, overdue = 0, total = 0, dueDate, onPay }) { const safeTotal = Number(total) || 1; return <article className="dashboard-card fee-card"><h2>Fees status</h2><div className="fee-progress"><i style={{width:`${Number(paid)/safeTotal*100}%`}}/><b style={{width:`${Number(pending)/safeTotal*100}%`}}/><em style={{width:`${Number(overdue)/safeTotal*100}%`}}/></div><dl><div><dt>Paid</dt><dd>{money(paid)}</dd></div><div><dt>Pending</dt><dd>{money(pending)}</dd></div><div><dt>Overdue</dt><dd>{money(overdue)}</dd></div></dl>{dueDate && <p className={overdue ? 'overdue' : ''}>Due: {dueDate}</p>}{onPay && <button className="dashboard-button" onClick={onPay}>Make payment</button>}</article> }
