export default function ActivityFeed({ activities = [], maxItems = 5, viewMoreLink }) { return <article className="dashboard-card activity-feed"><h2>Recent activity</h2>{activities.slice(0,maxItems).map((item, i) => <div className="activity" key={item.id || i}><span className={`activity-dot ${item.color || 'navy'}`}/><div><strong>{item.title}</strong><p>{item.description}</p><small>{item.timestamp}</small></div></div>)}{!activities.length && <div className="dashboard-empty">Nothing new yet. Activity will appear here as records are updated.</div>}{viewMoreLink && <a href={viewMoreLink}>View more activity</a>}</article> }
